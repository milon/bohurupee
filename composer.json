{
    "name": "milon/bohurupee",
    "description": "Local fake identity provider for OAuth and OIDC development. DEV ONLY.",
    "type": "project",
    "license": "MIT",
    "private": true,
    "require": {
        "php": "^8.2",
        "milon/papyrus": "^1.4"
    },
    "config": {
        "sort-packages": true
    },
    "scripts": {
        "docs": "papyrus build:site -d docs",
        "docs:serve": "papyrus serve -d docs --build"
    }
}
